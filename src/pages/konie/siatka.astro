---
import Page from '../../layouts/Page.astro';
import SubBar from '../../components/SubBar.astro';
import { t } from '../../i18n';

const locale = 'pl' as const;
const dict = t(locale);
const page = dict.pages.horsesGrid;
---

<Page locale={locale} routeKey="horsesGrid" title={page.title} description={page.description}>
  <SubBar label={page.label} />
  <section class="stub">
    <!-- Ticket 4.3 — three-up grid, toggled from the sub-bar. -->
    <p class="copy">4.3 — three-up grid, toggled from the sub-bar</p>
  </section>
</Page>

<style>
  .stub {
    padding: 80px var(--gutter);
    color: var(--ink-muted);
  }
</style>
